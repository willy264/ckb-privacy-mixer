'use strict';

throw new Error(
    'This legacy private-key signing probe is retired. Use the SDK tests and inject a CCC Client/Signer ' +
    'from the consuming application; never commit wallet keys to the repository.',
);
