import { DepositNote } from '../types/note';

export async function withdrawMix(note: DepositNote): Promise<string> {
    // Phase 3: Nullifier-based withdrawal pipeline
    throw new Error('Not implemented: Phase 3 nullifier withdrawal required');
}
